CREATE OR REPLACE EDITIONABLE PACKAGE "SERVAPP"."PKG_EXT" AS 

   PROCEDURE SP_EXTENSION(
      P_DATA IN BLOB,
      P_RESPONSE OUT CLOB );

    PROCEDURE MOVESTAMP(
      P_DATA IN BLOB,
      P_RESPONSE OUT CLOB );
      
END PKG_EXT;
/
