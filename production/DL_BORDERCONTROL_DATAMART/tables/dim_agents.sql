CREATE TABLE "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" 
   (	"ID" VARCHAR2(32 CHAR) NOT NULL ENABLE, 
	"NAME" VARCHAR2(128 CHAR), 
	"CREATE_TIMESTAMP" TIMESTAMP (6) WITH TIME ZONE NOT NULL ENABLE, 
	"MODIFY_TIMESTAMP" TIMESTAMP (6) WITH TIME ZONE, 
	"ICAO" VARCHAR2(3), 
	"IATA" VARCHAR2(2)
   ) ;
  ALTER TABLE "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" ADD CONSTRAINT "DIM_AGENTS_PK" PRIMARY KEY ("ID")
  USING INDEX  ENABLE;
  CREATE UNIQUE INDEX "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS_PK" ON "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" ("ID") 
  ;
  GRANT SELECT ON "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" TO "SERVAPP";
  GRANT SELECT ON "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" TO "BIO_BD";
  GRANT DELETE ON "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" TO "DERMALOG_PROXY";
  GRANT INSERT ON "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" TO "DERMALOG_PROXY";
  GRANT SELECT ON "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" TO "DERMALOG_PROXY";
  GRANT UPDATE ON "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" TO "DERMALOG_PROXY";
  GRANT SELECT ON "DL_BORDERCONTROL_DATAMART"."DIM_AGENTS" TO "APPSUP";
